# Story 2.1: Estrutura de Planos e Feature Flags

## Status
Draft

## Story

**As a** sistema,
**I want** ter estrutura de planos (Gratuito, Básico, Avançado, Profissional),
**so that** features sejam liberadas conforme o plano do usuário.

## Acceptance Criteria

1. Modelo de dados suporta diferentes níveis de plano
2. Feature flags controlam acesso a funcionalidades por plano
3. Middleware/guard verifica permissões antes de acessar features
4. Tela de upgrade disponível quando usuário tenta acessar feature bloqueada
5. Configuração de planos gerenciável sem deploy

## Tasks / Subtasks

- [ ] **Criar Tabela de Planos** (AC: 1)
  - [ ] Migration: tabela `plans` com features JSONB
  - [ ] Seed: inserir 4 planos (free, basic, advanced, professional)
  - [ ] Associar plano ao usuário

- [ ] **Configurar Flagsmith** (AC: 2, 5)
  - [ ] Setup projeto Flagsmith
  - [ ] Criar flags por feature:
    - `feature_daily_plan`
    - `feature_chat`
    - `feature_notifications`
    - `feature_voice_chat`
    - `feature_reports_advanced`
    - `feature_community`
    - `feature_professional_dashboard`
  - [ ] Configurar traits de usuário (plan_type)

- [ ] **Implementar PlanGuard** (AC: 3)
  - [ ] Criar `hooks/usePlanAccess.ts`
  - [ ] Verificar flag antes de renderizar
  - [ ] Wrapper component `<PlanGate>`

- [ ] **Tela de Upgrade** (AC: 4)
  - [ ] Criar `screens/Upgrade/UpgradeScreen.tsx`
  - [ ] Mostrar benefícios do próximo plano
  - [ ] CTA para upgrade
  - [ ] Usar quando feature bloqueada

- [ ] **Integração com Pagamento** (stub)
  - [ ] Placeholder para integração futura
  - [ ] Função `upgradePlan()` que simula

## Dev Notes

### Tabela plans
```sql
CREATE TABLE plans (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    name TEXT UNIQUE NOT NULL CHECK (name IN ('free', 'basic', 'advanced', 'professional')),
    display_name TEXT NOT NULL,
    features JSONB NOT NULL DEFAULT '{}',
    price_brl DECIMAL(10,2),
    price_usd DECIMAL(10,2),
    billing_period TEXT CHECK (billing_period IN ('monthly', 'yearly')),
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Seed
INSERT INTO plans (name, display_name, features, price_brl) VALUES
('free', 'Gratuito', '{"discovery": true}', 0),
('basic', 'Básico', '{"discovery": true, "daily_plan": true, "chat": true, "notifications_simple": true, "widgets": true, "crisis_mode": true}', 19.90),
('advanced', 'Avançado', '{"discovery": true, "daily_plan": true, "chat": true, "voice_chat": true, "notifications_advanced": true, "reports_advanced": true, "education": true, "community": true, "widgets": true, "crisis_mode": true}', 39.90),
('professional', 'Profissional', '{"all": true, "professional_link": true, "professional_dashboard": true}', 79.90);
```

### Hook usePlanAccess
```typescript
// hooks/usePlanAccess.ts
import { useFlags } from 'flagsmith/react';

interface PlanAccess {
  hasAccess: boolean;
  isLoading: boolean;
  requiredPlan: string;
}

export function usePlanAccess(featureKey: string): PlanAccess {
  const flags = useFlags([featureKey]);
  const flag = flags[featureKey];

  return {
    hasAccess: flag?.enabled ?? false,
    isLoading: flag === undefined,
    requiredPlan: flag?.value?.requiredPlan ?? 'basic',
  };
}
```

### PlanGate Component
```typescript
// components/common/PlanGate.tsx
interface PlanGateProps {
  feature: string;
  children: React.ReactNode;
  fallback?: React.ReactNode;
}

export function PlanGate({ feature, children, fallback }: PlanGateProps) {
  const { hasAccess, isLoading, requiredPlan } = usePlanAccess(feature);
  const navigation = useNavigation();

  if (isLoading) return <LoadingSpinner />;

  if (!hasAccess) {
    return fallback ?? (
      <UpgradePrompt
        requiredPlan={requiredPlan}
        onUpgrade={() => navigation.navigate('Upgrade')}
      />
    );
  }

  return <>{children}</>;
}
```

### Testing
- Testar que plano free só acessa discovery
- Testar que plano basic acessa features básicas
- Testar UI de upgrade aparece corretamente

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-23 | 1.0 | Criação da story | PO |
