# Story 3.1: Chat de Acompanhamento (Texto)

## Status
Draft

## Story

**As a** usuário,
**I want** interagir via chat para registrar como está meu dia,
**so that** o sistema colete sinais de forma natural.

## Acceptance Criteria

1. Interface de chat conversacional limpa
2. Perguntas curtas e gentis
3. Respostas por botões/quick replies (sem digitação obrigatória)
4. Máximo 1-2 interações por sessão
5. Tom sempre de convite, nunca cobrança
6. Histórico de conversas acessível

## Tasks / Subtasks

- [ ] **Implementar Chat Screen** (AC: 1, 6)
  - [ ] Criar `screens/Chat/ChatScreen.tsx`
  - [ ] Lista de mensagens (FlatList)
  - [ ] Input area (opcional)
  - [ ] Histórico scrollável

- [ ] **Componentes de Chat**
  - [ ] `components/chat/MessageBubble.tsx` (sistema/usuário)
  - [ ] `components/chat/QuickReplies.tsx`
  - [ ] `components/chat/TypingIndicator.tsx`

- [ ] **Quick Replies** (AC: 3)
  - [ ] Chips tocáveis abaixo da mensagem
  - [ ] Desaparecem após seleção
  - [ ] Feedback visual ao tocar

- [ ] **Fluxo de Conversa** (AC: 2, 4, 5)
  - [ ] Definir árvore de conversa
  - [ ] Máximo 2 perguntas por sessão
  - [ ] Encerramento gentil
  - [ ] Linguagem acolhedora

- [ ] **Persistência de Histórico** (AC: 6)
  - [ ] Tabela `chat_messages`
  - [ ] Carregar últimas conversas
  - [ ] Paginação

- [ ] **Integração com Check-in**
  - [ ] Chat pode iniciar check-in
  - [ ] Respostas alimentam sistema

## Dev Notes

### Fluxo de Conversa Exemplo
```
Sistema: "Oi Ana! 👋 Como está sendo seu dia até agora?"
[Chips: "Tranquilo", "Um pouco cansativo", "Difícil"]

Usuário: [toca "Um pouco cansativo"]

Sistema: "Entendo... dias assim acontecem. 💙 Sua energia está como agora?"
[Chips: "😴 Baixa", "😐 Média", "⚡ Alta"]

Usuário: [toca "😴 Baixa"]

Sistema: "Pronto! Ajustei seu plano para um dia mais leve. 🌟"
Sistema: "Cuide de você. Se precisar, o modo crise está aqui."
[Botão: "Ver plano ajustado"]
```

### Tabela chat_messages
```sql
CREATE TABLE chat_messages (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    role TEXT NOT NULL CHECK (role IN ('user', 'assistant')),
    content TEXT NOT NULL,
    quick_replies JSONB, -- para mensagens do sistema
    metadata JSONB DEFAULT '{}',
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_chat_messages_user ON chat_messages(user_id, created_at DESC);
```

### MessageBubble Styles
```typescript
// Sistema: branco, esquerda, rounded-2xl
// Usuário: primary (#4A90A4), direita, rounded-2xl

const bubbleStyles = {
  assistant: {
    backgroundColor: '#FFFFFF',
    alignSelf: 'flex-start',
    marginRight: 48,
  },
  user: {
    backgroundColor: '#4A90A4',
    alignSelf: 'flex-end',
    marginLeft: 48,
  },
};
```

### Tom de Voz
- "Como você está?" (não "Relate seu status")
- "Entendo..." (não "Registrado")
- "Dias assim acontecem" (não "Performance baixa")
- "Cuide de você" (não "Tente melhorar")

### Testing
- Mensagens renderizam corretamente
- Quick replies funcionam
- Histórico carrega
- Fluxo completa sem erros

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-23 | 1.0 | Criação da story | PO |
