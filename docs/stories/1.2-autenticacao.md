# Story 1.2: Autenticação e Gestão de Usuários

## Status
Draft

## Story

**As a** usuário,
**I want** poder criar conta e fazer login de forma simples,
**so that** meus dados sejam salvos com segurança.

## Acceptance Criteria

1. Registro com email/senha funcional
2. Login funcional com sessão persistente
3. Opção de login social (Google) disponível
4. Fluxo de recuperação de senha implementado
5. Sessão offline funciona para usuários já logados
6. LGPD: Termos de uso e política de privacidade no registro

## Tasks / Subtasks

- [ ] **Configurar Supabase Auth** (AC: 1, 2)
  - [ ] Habilitar email/password auth no Supabase
  - [ ] Criar tabela `users` que estende `auth.users`
  - [ ] Configurar RLS policies básicas

- [ ] **Implementar Telas de Auth (Mobile)** (AC: 1, 2)
  - [ ] Criar `screens/Auth/LoginScreen.tsx`
  - [ ] Criar `screens/Auth/RegisterScreen.tsx`
  - [ ] Implementar validação com Zod
  - [ ] Integrar com Supabase Auth SDK

- [ ] **Implementar Login Social** (AC: 3)
  - [ ] Configurar Google OAuth no Supabase
  - [ ] Adicionar botão "Continuar com Google"
  - [ ] Configurar deep links para callback

- [ ] **Fluxo de Recuperação de Senha** (AC: 4)
  - [ ] Criar `screens/Auth/ForgotPasswordScreen.tsx`
  - [ ] Implementar envio de email de reset
  - [ ] Criar tela de nova senha

- [ ] **Persistência de Sessão** (AC: 2, 5)
  - [ ] Configurar armazenamento seguro de tokens (SecureStore)
  - [ ] Implementar refresh automático de tokens
  - [ ] Testar sessão offline

- [ ] **LGPD Compliance** (AC: 6)
  - [ ] Criar checkbox de aceite de termos no registro
  - [ ] Criar páginas de Termos de Uso e Privacidade
  - [ ] Registrar aceite no banco de dados

- [ ] **Gestão de Estado de Auth**
  - [ ] Criar `stores/authStore.ts` com Zustand
  - [ ] Estados: loading, authenticated, user, error
  - [ ] Persist state com AsyncStorage

## Dev Notes

### Tabela users (SQL)
```sql
CREATE TABLE users (
    id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
    plan_id UUID REFERENCES plans(id) DEFAULT (SELECT id FROM plans WHERE name = 'free'),
    onboarding_completed BOOLEAN DEFAULT FALSE,
    terms_accepted_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- RLS
ALTER TABLE users ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can view own data" ON users
    FOR SELECT USING (auth.uid() = id);
CREATE POLICY "Users can update own data" ON users
    FOR UPDATE USING (auth.uid() = id);
```

### Supabase Auth Config
- Email confirmations: Desabilitado inicialmente (friction)
- Password min length: 8
- Google OAuth: Configurar em Supabase Dashboard

### Zustand Auth Store
```typescript
interface AuthState {
  user: User | null;
  session: Session | null;
  isLoading: boolean;
  isAuthenticated: boolean;
  signIn: (email: string, password: string) => Promise<void>;
  signUp: (email: string, password: string) => Promise<void>;
  signOut: () => Promise<void>;
  signInWithGoogle: () => Promise<void>;
}
```

### Validação (Zod Schemas)
```typescript
// packages/shared/src/schemas/auth.ts
export const loginSchema = z.object({
  email: z.string().email('Email inválido'),
  password: z.string().min(8, 'Mínimo 8 caracteres'),
});

export const registerSchema = loginSchema.extend({
  acceptTerms: z.literal(true, {
    errorMap: () => ({ message: 'Aceite os termos' }),
  }),
});
```

### Testing

- **Testes unitários:** Validação de schemas, store actions
- **Testes de integração:** Fluxo completo de registro e login
- **Localização:** `apps/mobile/src/screens/Auth/__tests__/`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-23 | 1.0 | Criação da story | PO |
