# Story 7.1: Supabase Auth e Setup

## Status
Draft

## Story

**As a** usuário do Mentes Brilhantes,
**I want** fazer login com Google ou Email,
**so that** meus dados sejam salvos na nuvem e eu possa acessar de qualquer dispositivo.

## Acceptance Criteria

1. Projeto Supabase configurado com Auth habilitado
2. Login com Google OAuth funcionando
3. Login com Email/Senha funcionando
4. Recuperação de senha por email
5. Sessão persistente (não desloga ao fechar app)
6. Migração de dados locais para cloud ao fazer primeiro login
7. Opção de continuar usando só local (Free)

## Tasks / Subtasks

- [ ] **Setup Supabase** (AC: 1)
  - [ ] Criar projeto no Supabase Dashboard
  - [ ] Configurar variáveis de ambiente (SUPABASE_URL, SUPABASE_ANON_KEY)
  - [ ] Instalar @supabase/supabase-js
  - [ ] Criar client singleton

- [ ] **Configurar Google OAuth** (AC: 2)
  - [ ] Criar projeto no Google Cloud Console
  - [ ] Configurar OAuth consent screen
  - [ ] Obter Client ID e Secret
  - [ ] Configurar provider no Supabase
  - [ ] Implementar botão "Login com Google"

- [ ] **Login Email/Senha** (AC: 3, 4)
  - [ ] Tela de cadastro com email/senha
  - [ ] Tela de login
  - [ ] Confirmação de email (opcional, configurável)
  - [ ] Recuperação de senha

- [ ] **Persistência de Sessão** (AC: 5)
  - [ ] Configurar storage adapter para sessão
  - [ ] Auto-refresh de token
  - [ ] Verificar sessão ao abrir app

- [ ] **Migração Local → Cloud** (AC: 6)
  - [ ] Detectar dados locais existentes ao fazer login
  - [ ] Perguntar se quer migrar
  - [ ] Copiar dados do localStorage para Supabase
  - [ ] Manter cópia local como cache

- [ ] **Modo Híbrido** (AC: 7)
  - [ ] Toggle "Usar apenas local" nas configurações
  - [ ] Banner "Faça login para sincronizar"
  - [ ] Funcionalidades completas mesmo sem login

## Dev Notes

### Schema Supabase

```sql
-- Tabela de perfis (extende auth.users)
CREATE TABLE profiles (
  id UUID REFERENCES auth.users PRIMARY KEY,
  name TEXT,
  avatar_url TEXT,
  plan TEXT DEFAULT 'free',
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- RLS
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can view own profile" ON profiles
  FOR SELECT USING (auth.uid() = id);
CREATE POLICY "Users can update own profile" ON profiles
  FOR UPDATE USING (auth.uid() = id);
```

### Variáveis de Ambiente

```env
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=xxx
```

### Fluxo de Migração

```
Usuário faz login pela primeira vez
         │
         ▼
Detecta localStorage com dados?
         │
    ┌────┴────┐
    │         │
   Sim       Não
    │         │
    ▼         ▼
Pergunta:    Criar
"Migrar?"    conta
    │        vazia
    ▼
   Sim → Upload dados
   Não → Ignora (começa fresh)
```

## Testing

- [ ] Login Google funciona
- [ ] Login email funciona
- [ ] Recuperação senha funciona
- [ ] Sessão persiste após fechar browser
- [ ] Migração de dados funciona
- [ ] Modo local-only funciona

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-26 | 1.0 | Criação da story | Claude |
