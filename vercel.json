{
  "buildCommand": "pnpm turbo run build --filter=@nciaflux/web",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@8.12.0 --activate && pnpm install"
}
